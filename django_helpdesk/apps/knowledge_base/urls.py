from django.urls import path

from . import views

urlpatterns = [

    #path('', use list class, name='index_knowledge_base'), SIDE BAR (FILTER)
    #path('details/<int:id>', use details class, name='details_knowledge_base'),
    #path('new', use forms, name='new_knowledge_base'),
 
]